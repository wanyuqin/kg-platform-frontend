# ADR-0001：Gateway 统一收口，Agent 不直连 OpenViking

- **状态**：已采纳
- **日期**：2026-07-03（设计评审期拍板）
- **来源**：设计文档 2.2、第六章

## 背景

OpenViking 本身没有鉴权概念，任何拿到地址的调用方都能读全部内容。

## 决策

所有 Agent 只能经 Knowledge Gateway（`/v1/*`）访问知识，OpenViking 部署在内网、不对消费方暴露。

## 理由

权限（domain 白名单）、限流、审计需要唯一收口；封装后存储引擎可替换，Agent 侧接口契约不受影响。
