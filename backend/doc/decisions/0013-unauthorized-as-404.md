# ADR-0013：越权与不存在统一返回 404

- **状态**：已采纳
- **日期**：2026-07-04
- **来源**：设计文档 6.1.3；第六章评审拍板

## 背景

`GET /v1/knowledge/{kid}` 在 kid 不存在、domain 越权、非 published、已过期四种情况下如何区分响应。

## 决策

四种情况统一返回 404 not_found，不区分。

## 理由

kid 携带 domain 与类型信息（如 faq-fo-0086），若越权返回 403 会向无权限方泄漏"该 kid 存在"这一事实；统一 404 不暴露存在性。
